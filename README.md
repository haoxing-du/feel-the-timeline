# feel-the-timeline
A site that helps you feel the AI timeline.

## Local setup

Requires Node.js 22 or newer. Copy `.env.example` to `.env.local`, add Replicate and OpenRouter API keys, then run:

```sh
npm install
npm run dev
```
